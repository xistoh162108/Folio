import { SettingsScreenBound } from "@/components/v0/admin/settings-screen-bound"

export const dynamic = "force-dynamic"

export default async function AdminSettingsPage() {
  return <SettingsScreenBound />
}
